[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "diffusion-ts"
version = "0.0.0"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["engine*", "Data*", "Models*", "Utils*"]
namespaces = true
